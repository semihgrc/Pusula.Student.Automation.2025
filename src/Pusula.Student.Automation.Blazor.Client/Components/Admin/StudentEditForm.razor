@using Microsoft.AspNetCore.Components.Forms
@using Pusula.Student.Automation.Enums
@using Pusula.Student.Automation.Students

<EditForm Model="@Model" OnValidSubmit="@OnValidSubmitInternal">
    <Microsoft.AspNetCore.Components.Forms.DataAnnotationsValidator />
    <Microsoft.AspNetCore.Components.Forms.ValidationSummary />

    <div class="row g-3">
        <div class="col-md-6">
            <label class="form-label">Ad</label>
            <input class="form-control"
                   @bind="Model.Name"
                   required
                   minlength="@StudentConsts.MinNameLength"
                   maxlength="@StudentConsts.MaxNameLength" />
        </div>
        <div class="col-md-6">
            <label class="form-label">Soyad</label>
            <input class="form-control"
                   @bind="Model.Surname"
                   required
                   minlength="@StudentConsts.MinSurnameLength"
                   maxlength="@StudentConsts.MaxSurnameLength" />
        </div>
        <div class="col-md-6">
            <label class="form-label">Cinsiyet</label>
            <select class="form-select" @bind="Model.Gender">
                @foreach (EnumGender gender in Enum.GetValues(typeof(EnumGender)))
                {
                    <option value="@gender">@gender</option>
                }
            </select>
        </div>
        <div class="col-md-6">
            <label class="form-label">Öğrenci No</label>
            <input class="form-control"
                   @bind="Model.StudentNumber"
                   required
                   minlength="@StudentConsts.MinStudentNumberLength"
                   maxlength="@StudentConsts.MaxStudentNumberLength" />
        </div>
        <div class="col-md-6">
            <label class="form-label">E-posta</label>
            <input class="form-control"
                   type="email"
                   @bind="Model.Email"
                   required
                   minlength="@StudentConsts.MinEmailLength"
                   maxlength="@StudentConsts.MaxEmailLength" />
        </div>
        <div class="col-md-6">
            <label class="form-label">Telefon</label>
            <input class="form-control"
                   @bind="Model.PhoneNumber"
                   required
                   minlength="@StudentConsts.MinPhoneNumberLength"
                   maxlength="@StudentConsts.MaxPhoneNumberLength" />
        </div>
    </div>

    <div class="mt-3 d-flex justify-content-between">
        @if (OnCancel.HasDelegate)
        {
            <button type="button" class="btn btn-outline-secondary" @onclick="OnCancelInternal">İptal</button>
        }
        <button class="btn btn-success ms-auto" type="submit" disabled="@IsSubmitting">Kaydet</button>
    </div>
</EditForm>

@code {
    [Parameter] public StudentUpdateDto Model { get; set; } = default!;
    [Parameter] public bool IsSubmitting { get; set; }
    [Parameter] public EventCallback OnValidSubmit { get; set; }
    [Parameter] public EventCallback OnCancel { get; set; }

    private Task OnValidSubmitInternal() => OnValidSubmit.InvokeAsync(null);
    private Task OnCancelInternal() => OnCancel.InvokeAsync(null);
}
